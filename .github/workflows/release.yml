# ================================================================
#  AUTO BUILD & RELEASE WORKFLOW
# ================================================================
#  HOW TO USE — only change ONE thing:
#    1. Copy to your repo: .github/workflows/release.yml
#    2. Set PROJECT_NAME below to neovector repo / binary name
#    3. Make sure your repo root has a `.version` file, e.g:  1.0.0
#    4. Push a tag:  git tag vx.x.x && git push --tags
#
#  BINARY OUTPUT NAMES:
#    neovector-windows-amd64.exe    (Windows machine type 9  = AMD64)
#    neovector-windows-arm64.exe    (Windows machine type 12 = ARM64)
#    neovector-linux-amd64
#    neovector-linux-arm64
#    neovector-darwin-amd64
#    neovector-darwin-arm64
#
#  LDFLAGS INJECTED INTO BINARY:
#    main.Version        <- fetched from .version file
#    main.Commit         <- short git SHA (8 chars)
#    main.PublisherName  <- rkriad585
#    main.PublisherEmail <- rkriad585@gmail.com
#
#  REQUIRED Go variable declarations in your main package:
#    var (
#        Version        string
#        Commit         string
#        PublisherName  string
#        PublisherEmail string
#    )
# ================================================================

name: Auto Build & Release

# ── ✏️  ONLY EDIT THESE TWO LINES ─────────────────────────────
env:
  PROJECT_NAME:    neovector           # <- neovector repo name / binary name
  GO_VERSION:      "1.25.0"            # <- your project's Go version
# ─────────────────────────────────────────────────────────────

  PUBLISHER_NAME:  rkriad585
  PUBLISHER_EMAIL: rkriad585@gmail.com

# ── TRIGGER ───────────────────────────────────────────────────
on:
  push:
    tags:
      - 'v*'                     # v1.0.0 | v2.3.1-beta | v0.0.1-rc.2
  workflow_dispatch:             # manual run from GitHub Actions UI

# ── GLOBAL PERMISSIONS ────────────────────────────────────────
permissions:
  contents: write                # create Releases + upload assets

# ==============================================================
# JOB 1 — Prepare: fetch version, commit, flags
# ==============================================================
jobs:
  prepare:
    name: "📋 Prepare metadata"
    runs-on: ubuntu-latest
    outputs:
      version:    ${{ steps.meta.outputs.version }}
      tag:        ${{ steps.meta.outputs.tag }}
      commit:     ${{ steps.meta.outputs.commit }}
      prerelease: ${{ steps.meta.outputs.prerelease }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch version + resolve metadata
        id: meta
        run: |
          OWNER="rkriad585"
          PROJECT="${{ env.PROJECT_NAME }}"

          REMOTE_URL="https://raw.githubusercontent.com/${OWNER}/${PROJECT}/main/.version"
          echo "Fetching version from: ${REMOTE_URL}"
          VERSION=$(curl -fsSL "${REMOTE_URL}" 2>/dev/null | tr -d '[:space:]') || true

          if [ -z "$VERSION" ] && [ -f ".version" ]; then
            VERSION=$(cat .version | tr -d '[:space:]')
            echo "Using local .version: $VERSION"
          fi

          if [ -z "$VERSION" ]; then
            TAG_TMP="${GITHUB_REF#refs/tags/}"
            VERSION="${TAG_TMP#v}"
            echo "Using git tag fallback: $VERSION"
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v${VERSION}")
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi

          COMMIT=$(git rev-parse --short=8 HEAD)

          if [[ "$TAG" == *"-"* ]]; then PRERELEASE="true"; else PRERELEASE="false"; fi

          echo "version=$VERSION"       >> "$GITHUB_OUTPUT"
          echo "tag=$TAG"               >> "$GITHUB_OUTPUT"
          echo "commit=$COMMIT"         >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"

          echo "## Release Metadata"                        >> "$GITHUB_STEP_SUMMARY"
          echo "| Field          | Value |"                 >> "$GITHUB_STEP_SUMMARY"
          echo "|----------------|-------|"                 >> "$GITHUB_STEP_SUMMARY"
          echo "| Project        | \`${PROJECT}\` |"        >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag            | \`${TAG}\` |"            >> "$GITHUB_STEP_SUMMARY"
          echo "| Version        | \`${VERSION}\` |"        >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit         | \`${COMMIT}\` |"         >> "$GITHUB_STEP_SUMMARY"
          echo "| Pre-release    | ${PRERELEASE} |"         >> "$GITHUB_STEP_SUMMARY"
          echo "| Publisher      | ${OWNER} |"              >> "$GITHUB_STEP_SUMMARY"

# ==============================================================
# JOB 2 — Build binaries for all 6 platform targets
# ==============================================================
  build:
    name: "🔨 Build ${{ matrix.label }}"
    needs: prepare
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - label:    windows/amd64
            runner:   windows-latest
            goos:     windows
            goarch:   amd64
            ext:      .exe
            cc:       ''

          - label:    windows/arm64
            runner:   windows-latest
            goos:     windows
            goarch:   arm64
            ext:      .exe
            cc:       ''

          - label:    linux/amd64
            runner:   ubuntu-latest
            goos:     linux
            goarch:   amd64
            ext:      ''
            cc:       ''

          - label:    linux/arm64
            runner:   ubuntu-latest
            goos:     linux
            goarch:   arm64
            ext:      ''
            cc:       aarch64-linux-gnu-gcc

          - label:    darwin/amd64
            runner:   macos-latest
            goos:     darwin
            goarch:   amd64
            ext:      ''
            cc:       ''

          - label:    darwin/arm64
            runner:   macos-latest
            goos:     darwin
            goarch:   arm64
            ext:      ''
            cc:       ''

    steps:
      - name: Checkout at ${{ needs.prepare.outputs.tag }}
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Set up Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install aarch64 cross-compiler
        if: matrix.goarch == 'arm64' && matrix.goos == 'linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build binary
        shell: bash
        env:
          GOOS:        ${{ matrix.goos }}
          GOARCH:      ${{ matrix.goarch }}
          CGO_ENABLED: 0
          CC:          ${{ matrix.cc }}
        run: |
          PROJECT="${{ env.PROJECT_NAME }}"
          VERSION="${{ needs.prepare.outputs.version }}"
          COMMIT="${{ needs.prepare.outputs.commit }}"
          PUB_NAME="${{ env.PUBLISHER_NAME }}"
          PUB_EMAIL="${{ env.PUBLISHER_EMAIL }}"

          OUTFILE="${PROJECT}-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}"

          LDFLAGS="-s -w"
          LDFLAGS="${LDFLAGS} -X 'main.Version=${VERSION}'"
          LDFLAGS="${LDFLAGS} -X 'main.Commit=${COMMIT}'"
          LDFLAGS="${LDFLAGS} -X 'main.PublisherName=${PUB_NAME}'"
          LDFLAGS="${LDFLAGS} -X 'main.PublisherEmail=${PUB_EMAIL}'"

          echo "-------------------------------------------"
          echo " Binary : ${OUTFILE}"
          echo " GOOS   : ${{ matrix.goos }}"
          echo " GOARCH : ${{ matrix.goarch }}"
          echo " Version: ${VERSION}"
          echo " Commit : ${COMMIT}"
          echo "-------------------------------------------"

          go build -trimpath -ldflags "${LDFLAGS}" -o "${OUTFILE}" .

          ls -lh "${OUTFILE}"
          echo "OUTFILE=${OUTFILE}" >> "$GITHUB_ENV"

      - name: Generate SHA-256 checksum
        shell: bash
        run: |
          if command -v sha256sum &>/dev/null; then
            sha256sum "${{ env.OUTFILE }}" > "${{ env.OUTFILE }}.sha256"
          else
            shasum -a 256 "${{ env.OUTFILE }}" > "${{ env.OUTFILE }}.sha256"
          fi
          cat "${{ env.OUTFILE }}.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            ${{ env.OUTFILE }}
            ${{ env.OUTFILE }}.sha256
          retention-days: 1
          if-no-files-found: error

# ==============================================================
# JOB 3 — Generate changelog from git commit history
# ==============================================================
  changelog:
    name: "📝 Generate Changelog"
    needs: prepare
    runs-on: ubuntu-latest
    outputs:
      body: ${{ steps.gen.outputs.body }}

    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build release notes
        id: gen
        shell: bash
        run: |
          TAG="${{ needs.prepare.outputs.tag }}"
          VERSION="${{ needs.prepare.outputs.version }}"
          COMMIT="${{ needs.prepare.outputs.commit }}"
          PROJECT="${{ env.PROJECT_NAME }}"

          PREV=$(git tag --sort=-creatordate | grep -v "^${TAG}$" | head -n 1 || true)
          RANGE="${PREV:+${PREV}..${TAG}}"
          RANGE="${RANGE:-HEAD}"

          FEAT=$(git log $RANGE --pretty=format:"- %s (%h)" | grep -iE "^\- feat(\(.+\))?:"  || true)
          FIX=$( git log $RANGE --pretty=format:"- %s (%h)" | grep -iE "^\- fix(\(.+\))?:"   || true)
          PERF=$(git log $RANGE --pretty=format:"- %s (%h)" | grep -iE "^\- perf(\(.+\))?:"  || true)
          DOCS=$(git log $RANGE --pretty=format:"- %s (%h)" | grep -iE "^\- docs(\(.+\))?:"  || true)
          REST=$(git log $RANGE --pretty=format:"- %s (%h)" | grep -ivE "^\- (feat|fix|perf|docs)(\(.+\))?:" || true)

          {
            echo 'body<<RELEASE_BODY_EOF'

            echo "## \`${PROJECT}\` ${TAG}"
            echo ""
            echo "> Commit: \`${COMMIT}\`  |  Publisher: **${{ env.PUBLISHER_NAME }}** \`${{ env.PUBLISHER_EMAIL }}\`"
            echo ""

            [ -n "$FEAT" ] && printf "### ✨ New Features\n%s\n\n" "$FEAT"
            [ -n "$FIX"  ] && printf "### 🐛 Bug Fixes\n%s\n\n"  "$FIX"
            [ -n "$PERF" ] && printf "### ⚡ Performance\n%s\n\n"   "$PERF"
            [ -n "$DOCS" ] && printf "### 📖 Documentation\n%s\n\n" "$DOCS"
            [ -n "$REST" ] && printf "### 🔧 Other Changes\n%s\n\n" "$REST"

            echo "---"
            echo ""
            echo "## 📥 Download Binaries"
            echo ""
            echo "| Platform | Architecture | File |"
            echo "|----------|-------------|------|"
            echo "| 🪟 Windows | AMD64 \`(machine type 9)\`  | \`${PROJECT}-windows-amd64.exe\` |"
            echo "| 🪟 Windows | ARM64 \`(machine type 12)\` | \`${PROJECT}-windows-arm64.exe\` |"
            echo "| 🐧 Linux   | AMD64                     | \`${PROJECT}-linux-amd64\`       |"
            echo "| 🐧 Linux   | ARM64                     | \`${PROJECT}-linux-arm64\`       |"
            echo "| 🍎 macOS   | AMD64 (Intel)             | \`${PROJECT}-darwin-amd64\`      |"
            echo "| 🍎 macOS   | ARM64 (Apple Silicon)     | \`${PROJECT}-darwin-arm64\`      |"
            echo ""
            echo "## ✅ Verify Checksum"
            echo ""
            echo "\`\`\`bash"
            echo "# Linux / macOS"
            echo "sha256sum --check ${PROJECT}-linux-amd64.sha256"
            echo ""
            echo "# Windows (PowerShell)"
            echo "Get-FileHash .\\${PROJECT}-windows-amd64.exe -Algorithm SHA256"
            echo "\`\`\`"
            echo ""
            echo "## 🔖 Build Info"
            echo ""
            echo "| Field          | Value |"
            echo "|----------------|-------|"
            echo "| Version        | \`${VERSION}\` |"
            echo "| Commit         | \`${COMMIT}\`  |"
            echo "| Publisher      | \`${{ env.PUBLISHER_NAME }}\` |"
            echo "| Go Version     | \`${{ env.GO_VERSION }}\`   |"
            echo "| Source         | https://raw.githubusercontent.com/rkriad585/${PROJECT}/main/.version |"

            echo 'RELEASE_BODY_EOF'
          } >> "$GITHUB_OUTPUT"

# ==============================================================
# JOB 4 — Publish the GitHub Release
# ==============================================================
  release:
    name: "🚀 Publish Release"
    needs: [prepare, build, changelog]
    runs-on: ubuntu-latest

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: bin-*
          path: dist/
          merge-multiple: true

      - name: List assets
        run: |
          echo "## Release Assets"   >> "$GITHUB_STEP_SUMMARY"
          echo "| File | Size |"     >> "$GITHUB_STEP_SUMMARY"
          echo "|------|------|"     >> "$GITHUB_STEP_SUMMARY"
          find dist/ -type f | sort | while read f; do
            SIZE=$(du -sh "$f" | cut -f1)
            echo "| \`$(basename $f)\` | $SIZE |" >> "$GITHUB_STEP_SUMMARY"
          done
          ls -lhR dist/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name:               ${{ needs.prepare.outputs.tag }}
          name:                   "${{ env.PROJECT_NAME }} ${{ needs.prepare.outputs.tag }}"
          body:                   ${{ needs.changelog.outputs.body }}
          prerelease:             ${{ needs.prepare.outputs.prerelease }}
          draft:                  false
          generate_release_notes: false
          files:                  dist/**
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# ==============================================================
# JOB 5 — Notify on failure
# ==============================================================
  notify-on-failure:
    name: "🔔 Failure Alert"
    needs: [prepare, build, changelog, release]
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Write failure summary
        run: |
          echo "## Release FAILED — ${{ env.PROJECT_NAME }} ${{ needs.prepare.outputs.tag }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Check the failed jobs above for details."                                       >> "$GITHUB_STEP_SUMMARY"
